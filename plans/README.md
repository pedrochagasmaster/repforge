# RepForge improvement plans

Generated by an `/improve deep` pass that transforms
[`docs/persona-product-feedback-report.md`](../docs/persona-product-feedback-report.md)
into self-contained, executable implementation plans, complemented with a
codebase audit of `app.js`, `index.html`, `styles.css`, and `sw.js`.

- **Written against**: commit `1d68b68` (`1d68b6821c5476740d57436a4571e2861696bac3`), 2026-07-01.
- **Codebase shape**: static PWA, no build step, no framework. One `localStorage`
  blob (`repforge_v1`) plus a draft key (`repforge_draft_v1`). Logic lives in
  `app.js` (~490 lines), markup in `index.html`, styles in `styles.css`,
  offline caching in `sw.js`.
- **Verification gate (every plan uses it)**: `node --check app.js` for syntax,
  then the Playwright simulation:

  ```bash
  # terminal 1 — static server from repo root
  python3 -m http.server 8000
  # terminal 2 — from repo root
  cd test && npm install && npx playwright install chromium && node simulation.mjs
  # expect: "FAILED: 0" and exit code 0
  ```

  There is **no** linter, type-checker, or bundler in this repo — do not look
  for one. `test/simulation.mjs` is the whole test surface (61 checks at the
  planning commit); executors add checks there for new behavior.

Every plan is written for the weakest plausible executor: exact `file:line`
current-state excerpts, repo conventions inlined, a drift check, verification
gates, hard scope boundaries, and STOP conditions. Plans never assume you saw
this index or the report.

## How to read a plan

Each file states its priority, effort, risk, category, dependencies, and the
persona(s) or audit finding it came from. Pick one, hand it to an executor
("implement `plans/001-*.md`"), and review the diff against its Done criteria.

## Product guardrails (from the report synthesis, treat as non-negotiable)

These constrain **every** plan. An executor that finds itself building any of
these should hit a STOP condition:

- No accounts, no cloud sync, no backend. Local-first is the product.
- No social feed, leaderboards, sharing, or gamification (streaks/badges/XP).
- No AI chat coach or auto-generated programs.
- No sixth nav tab; new depth ships hidden/collapsed/off the Log tab.
- Protect the Log tab's speed above all else.

## Priority order

Leverage = impact ÷ effort, weighted by confidence and by how many personas
raised it. Do foundational plans before dependents (see graph).

| # | Plan | Category | Effort | Risk | Source |
|---|------|----------|--------|------|--------|
| 016 | [Per-set "Save set" commit + suggested-vs-completed clarity](./016-per-set-commit-and-suggestion-clarity.md) | direction / dx | M | MED | **Operator feedback on 001** + session-loop thesis (power user, parent, minimalist) |
| 001 | [Rest timer](./001-rest-timer.md) | direction | M | LOW | Power user, parent, minimalist (report §5 "Next 2 weeks") |
| 002 | [Recommendation legibility + glossary-on-tap](./002-recommendation-legibility-glossary.md) | dx / direction | M | LOW | Beginner, coach (report §2.4) |
| 003 | [Hide progression dials behind Advanced](./003-hide-advanced-progression-settings.md) | dx | S | LOW | Beginner, minimalist |
| 004 | [Backup hygiene: last-export + import preview + Web Share](./004-backup-hygiene-import-preview.md) | correctness / dx | M | MED | Privacy maximalist, minimalist |
| 005 | [Skip exercise + fatigue→trim session](./005-skip-and-trim-session.md) | direction | M | MED | Parent, power user, coach |
| 006 | [Preserve exercise IDs in JSON editor (bug)](./006-preserve-exercise-ids-json-editor.md) | correctness | S | MED | **Audit** (grounds power-user/minimalist "rename orphans history") |
| 007 | [Rich analysis CSV export](./007-rich-analysis-csv-export.md) | direction / dx | S | LOW | Spreadsheet maximalist, coach |
| 008 | [kg / lbs unit toggle](./008-unit-toggle-kg-lbs.md) | direction | M | MED | Power user, spreadsheet |
| 009 | [Bodyweight per session](./009-bodyweight-per-session.md) | direction | S | LOW | Spreadsheet maximalist |
| 010 | [Redraw chart on resize/orientation (bug)](./010-chart-resize-redraw.md) | correctness | S | LOW | **Audit** |
| 011 | [Service-worker freshness for app shell (bug)](./011-service-worker-freshness.md) | correctness / dx | S | MED | **Audit** |
| 012 | [Accessibility: tab/nav state + focus](./012-accessibility-tabs-nav.md) | dx | S | LOW | **Audit** |
| 013 | [Session mode: one exercise at a time](./013-session-mode-focus.md) | direction | L | MED | Power user, parent, minimalist (report §2.1) |
| 014 | [IndexedDB durability migration](./014-indexeddb-durability.md) | tech-debt | L | HIGH | Privacy maximalist (report §2.3) |
| 015 | [Collapse Stats to an actionable default](./015-stats-collapse-actionable.md) | direction | M | LOW | Minimalist, spreadsheet |

## Dependency graph

```
006 (stable exercise IDs) ──┬─▶ 007 (CSV carries exercise_id)
                            └─▶ 013 (session mode keys off IDs)

016 (per-set commit) ──┬─▶ 001 (auto-start rest on set commit)
                       └─▶ 013 (focus mode reuses commit state)

008 (unit toggle) ──▶ 009 (bodyweight uses the same unit plumbing)

003 (Advanced settings) ──▶ 002 (glossary can live beside the dials)

014 (IndexedDB) is independent but touches load()/save()/import — land it
    AFTER 004 (backup hygiene) so import preview logic isn't rewritten twice.

005, 010, 011, 012, 015 are independent — any order.
```

Recommended first tranche (highest leverage, static-scope): **006 → 016 → 001 →
003 → 002 → 010 → 011 → 012 → 004 → 007**. Then the bigger bets:
**005 → 015 → 008 → 009 → 013 → 014**.

## Backlog (not yet planned in detail)

Grounded in the report but deferred — each needs its own plan before execution.
Recorded here so a `reconcile` pass doesn't re-flag them as new findings.

| Item | Source | Why deferred |
|------|--------|--------------|
| Substitution flow (approved alternates per slot, stats roll up) | Parent, coach, power user | Depends on 006 (stable IDs) and a slot model that doesn't exist yet. |
| Short / "essentials only" workout builder (time or readiness → 2–3 lifts) | Parent | Builds on 005 (trim) once trim ships and proves the hide-from-log mechanic. |
| Mesocycle blocks (tag sessions with block/week/phase; filter stats) | Spreadsheet maximalist | Large data-model change; sequence after 006 + 015 land. |
| PR ledger per exercise (load/rep/e1RM PRs with dates) | Spreadsheet maximalist, minimalist | Sits on top of 007's computed columns; scope separately. |
| Flexible schedule (missed-day queue instead of rigid day tabs) | Parent | Reworks the day-tab model in `renderTabs`; high blast radius. |
| Import merge by session id (vs. nuclear replace) | Privacy maximalist | Land after 004 + 014 so merge is written once against IndexedDB. |
| Optional client-side encrypted export (Web Crypto passphrase) | Privacy maximalist | Independent; needs its own crypto plan and STOP conditions. |
| Warmup vs. working-set flag (exclude warmups from volume) | Power user | Small, but touches the save schema and every volume calc; scope carefully. |
| Opt-in coach snapshot (share last N days read-only) | Coach | Conditional per guardrails — only after solo loop is excellent. |
| Beginner program variant (plain machine names, gym-floor hints) | Beginner, coach | Content + a program-picker; pairs well with 002. |

## Explicitly rejected (do not re-file)

Consensus across ≥5 personas that these betray the product:

- Social feed / leaderboards / PR sharing / community.
- AI coaching chat, auto-generated programs.
- Full coach SaaS (billing, scheduling, nutrition, in-app messaging).
- Wearable / HRV / sleep / readiness dashboards.
- Exercise GIF/video library, form-check empire.
- Gamification: streaks, badges, XP, "forge level".
- Mandatory accounts / "RepForge Cloud" as the default story.
- A marketplace of programs. A sixth nav tab.

## Status

| Plan | Status |
|------|--------|
| 001 | DONE |
| 002 | DONE |
| 003 | DONE |
| 004 | DONE |
| 005 | DONE |
| 006 | DONE |
| 007 | DONE |
| 008 | DONE |
| 009 | DONE |
| 010 | DONE |
| 011 | DONE |
| 012 | DONE |
| 013 | DONE |
| 014 | DONE |
| 015 | DONE |
| 016 | DONE |
