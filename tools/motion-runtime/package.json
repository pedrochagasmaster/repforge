{
  "name": "taurifer-motion-runtime-build",
  "private": true,
  "version": "1.0.0",
  "description": "Offline generator dependencies for vendor/motion/motion.js. Tooling only — the application itself ships the committed bundle and has no package manager.",
  "type": "module",
  "dependencies": {
    "esbuild": "0.25.10",
    "motion": "13.2.0"
  }
}
