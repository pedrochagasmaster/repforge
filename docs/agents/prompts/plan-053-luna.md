# Start Plan 053 with native Luna workers

Send this prompt to the coordinating Codex agent. Select Fast in the host
configuration if that is where child service tiers are controlled; the agent
must verify the effective child setting.

```text
Implement Taurifer Plan 053 in pedrochagasmaster/repforge.
You coordinate and judge; use native Luna Max Fast subagents for all delegated
implementation, tests, review, and integration. Do not use Herdr or other models.

Read root/nested AGENTS.md, the complete
plans/053-ios-install-transfer-foundation.md, its Native Luna worker protocol,
docs/adr/0013-temporary-install-transfer.md,
docs/agents/implementation-evidence.md, the Plan 053 checkpoint in
docs/agents/ui-overhaul-proof-checkpoints.md, and the cross-plan sequence.
The Plan 053 native override takes precedence over shared Herdr/Gemini routing.
Read the required clone/privacy/telemetry contracts before implementing consumers.

Fetch origin/main. Inspect branches, worktrees, active workers, open PRs, and
PR #228's current state. Resume existing 053 work if present. Otherwise create
branch ui-overhaul/053-ios-install-transfer with dedicated worktree
../repforge-ui-053-install-transfer, push a kickoff commit, and open/populate
the plan's draft PR before substantive implementation. Target main.

Select the available Luna model, expected gpt-5.6-luna, reasoning effort max,
and verify Fast service through the supported native/session configuration.
Record actual child settings; do not invent a Fast tool argument or silently
substitute a setting/model. If Fast cannot be selected or verified, complete
coordinator preparation and report the precise limitation before dispatching.

Maximize useful concurrency under the plan's Waves A–E. Fill available worker
slots with independent ready assignments. Give each writer its own branch and
worktree. Keep one writer on each mutable contract and one designated Luna
integrator on the integration checkout. Record worker IDs, owned paths, SHAs,
ports and PIDs in the PR. Workers do not recursively delegate.

Start in parallel with the actor/credential/fault oracle, acknowledged-clone
characterization, and exact limits/negative-case design. Reconcile the shared
interfaces before parallel service, isolated browser-client, and adversarial
test implementation. Service create/claim/commit/status share one serial owner.
Only the integrator edits app.js, shell/cache, generated i18n, shared browser
helpers and catalog files. Merge reviewed published worker branches one at a
time; rerun affected consumer proof. Never copy unpublished sibling work.

Plan 052 may still be changing storage. Continue isolated service/client/test
packets now; serialize app/storage integration after 052 merges, explicitly
merge current main, and preserve its durable transition/recovery fields in
the clone. Respect the existing DraftV2 checkpoint, CAS, queue-drain, and
program-replacement boundaries. Confirmed no-draft state transfers null;
untrustworthy active state cannot be silently dropped.

Keep every product decision in ADR 0013 fixed: Cloudflare/EU processing,
exact logical clone, token-derived encryption, exact body endpoints, same-claim
retry, verified-import commit deletion, 60-minute live payload lifetime,
75-minute tombstone deadline, atomic import, Safari recovery/divergence rules,
privacy and consent/identity preservation. No accounts or generalized sync.

Prepare expected assertions and negative cases before production changes.
Test actual producers/consumers and both actors at each network/local-write
boundary. Independent expected values must not come from the code under test.
Use focused proof before expensive regression; preserve required full suites,
catalog, staging and physical-device gates. Never claim physical iOS evidence
from emulation. Keep private payloads/tokens out of logs and artifacts.

Push each coherent tested slice immediately, update the PR with actual SHAs,
test results, fault evidence, worker ownership and Next exact steps. Review
the complete affected contract before returning findings. If a repair repeats
the same defect, resolve the state model and oracle before another Luna attempt.
Use native follow-ups without status-only interruptions. No published-history
rewrites, force pushes, knowingly broken checkpoints, or deletion of others' work.

Continue all independent authorized work until Plan 053 is ready for owner
review or a required owner/environment gate remains. Report its exact blocker
and evidence. Do not merge the implementation PR or deploy to production without
explicit owner authorization. Finish with remote SHA, clean worktree status,
completed packets, focused/full checks, staging/device gates and exact next action.
```
